{
  "name": "makaziplus",
  "version": "2.0.0",
  "description": "MakaziPlus - Tanzania's #1 Property Platform",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n SERVER,CLIENT -c green,cyan \"npm run server\" \"npm run client\"",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client start",
    "install-all": "npm install && npm --prefix server install && npm --prefix client install",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  }
}
